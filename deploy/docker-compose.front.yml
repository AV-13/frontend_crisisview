services:
  frontend:
    image: ${FRONTEND_IMAGE:-crisiview-frontend:latest}
    container_name: crisiview-frontend
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    ports:
      - "3000:3000"
    restart: unless-stopped
